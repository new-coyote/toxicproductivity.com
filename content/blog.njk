---
layout: app.njk
permalink: "blog/"
---

<header class="mb-12">
    <h1 class="text-4xl">Blog</h1>
</header>

<div>
    {% for post in collections.post %}
        <article class="mb-8">
            <h2 class="mb-2 text-2xl hover:text-purple-600"><a href="{{ post.url }}">{{ post.data.title }} »</a></h2>
            <div class="prose prose-xl">
                <div class="prose-sm">{{ post.date.toLocaleDateString('us-EN', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) }}</div>
                {{ post.data.excerpt | safe }}
                <div>

                </div>
            </div>
        </article>
    {% endfor %}
</div>
